#include <stdio.h>
int main()
{
    int length;
    int width;
    printf("Enter the Length:");
    scanf("%d", &length);
    printf("Enter the Width:");
    scanf("%d", &width);
    printf("The Area of Rectangle is %d", length * width);
    return 0;
}
